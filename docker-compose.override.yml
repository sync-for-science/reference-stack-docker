version: '2'

services:
  smart:
    environment:
     - BASE_URL=http://localhost:9000
    ports:
     - "9000:80"
  app:
    ports:
     - "8000:80"
  api:
    ports:
     - "9002:8080"
  tests:
    ports:
     - "9003:5000"
